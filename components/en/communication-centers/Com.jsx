import CenteredSection from "@/templates/CenteredSection";
import React from "react";

const Com = () => {
  return (
    <CenteredSection
      title={"Communication Centers"}
      description={
        "We install, extend and configure network telephone devices to communicate with members of the network, company or branches within the same network via telephone."
      }
    />
  );
};

export default Com;
