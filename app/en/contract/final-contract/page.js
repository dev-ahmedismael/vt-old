import FinalContract from "@/components/en/contract/FinalContract";
import React from "react";

const page = () => {
  return (
    <main>
      <FinalContract />
    </main>
  );
};

export default page;
